package DSAInterviewCourse.video.v09_conditional_and_loops;

public class Conditionals {

    public static void main(String[] args) {
        /*
            Demonstrating if-else statement syntax and usage:
            The if block is executed if the boolean expression is true, otherwise the else block is executed.
        */

        int salary = 25400;

        // Uncomment the code below to test basic if-else logic for adjusting salary based on conditions.

//        if (salary > 10000) {
//            salary = salary + 2000; // Increases salary by 2000 if above 10000
//        } else {
//            salary = salary + 1000; // Otherwise increases salary by 1000
//        }

        // Multiple if-else condition to adjust salary based on different ranges:

//        if (salary > 10000) {
//            salary += 2000; // salary = salary + 2000
//        } else if (salary > 20000) {
//            salary += 3000; // Increases salary by 3000 if greater than 20000
//        } else {
//            salary += 1000; // Otherwise, increase by 1000
//        }
//
//        System.out.println(salary); // Output the final salary value

        int a = 10;
        int b = 40;

        // Conditional check for a value not equal to 35 to print a message
        if (a != 35) {
            System.out.println("Hello World");
        }
    }
}
